const db = import('../models/index');
const CustomError = require(`../errors/index.js`);

const createBasket_Product = async (req, res) => {
  const user_id = req.userId;

  
  
};

module.exports = { createBasket_Product };
